import { comp } from 'csscomp'

export const Label = comp.span(
  {
    display: 'inline-flex',
    alignItems: 'center',
    paddingX: 'xs',
    border: 'main',
    borderColor: 'primary',
    borderRadius: 'm',
    backgroundColor: 'primary',
    color: 'white',
    fontSize: 's'
  }
)
